<!DOCTYPE html>
<html>

<head>
    <title>Nouveau Post</title>
</head>

<body>
    <h1>{{ $title }}</h1>
    <h2>{{ $sub_title }}</h2>
    <p>{{ $contenus }}</p>
    <img src="{{ $images }}" alt="Image du post">
</body>

</html>
